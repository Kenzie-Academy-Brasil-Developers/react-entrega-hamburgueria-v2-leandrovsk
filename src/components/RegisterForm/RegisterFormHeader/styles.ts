import styled from "styled-components";

export const StyledRegisterFormHeader = styled.span`
    width: 100%;
    display: flex;
    justify-content: space-between;
    margin-bottom: 2.2rem;

    h3 {
        color: var(--color-gray-3);
        font-size: 1.8rem;
        font-weight: 700;
    }
    
    a{
        color: var(--color-gray-2);
        font-size: 1.4rem;
    }
`
import styled from "styled-components";

export const StyledSideDesc = styled.aside`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 15px;

    .logo {
        padding-left: 20px;
    }
`